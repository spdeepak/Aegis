name: Go JWT Server CI

on:
  push:
    branches: [ "**" ]
  pull_request:
    branches: [ "**" ]

jobs:
  build-and-test:
    permissions:
      contents: read
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: 1.25
          cache: true

      - name: Start dependency stack
        run: |
          docker compose up -d --build
          docker compose ps

      - name: Run code generation
        run: make generate

      - name: Build
        run: go build ./...
        working-directory: apps/server

      - name: Test
        run: |
          go test -p 1 ./... -coverprofile=coverage.raw
          grep -vE "(mock|generated|cmd/main.go)" coverage.raw > coverage.out
        working-directory: apps/server

      - name: Upload to Codecov
        uses: codecov/codecov-action@v4
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          files: apps/server/coverage.out

      - name: Shut down stack
        if: always()
        run: docker compose down -v

      - name: Delete old workflow runs (keep last 2)
        if: success()
        continue-on-error: true
        env:
          GH_TOKEN: ${{ secrets.GH_PAT }}
        run: |
          echo "Deleting previous workflow runs..."
          gh run list \
            --workflow "Go JWT Server CI" \
            --limit 100 \
            --json databaseId,createdAt \
            -q 'sort_by(.createdAt) | reverse | .[2:] | .[].databaseId' |
          while read -r run_id; do
            echo "Deleting run $run_id"
            gh run delete "$run_id" --repo "${{ github.repository }}" || echo "Skipped $run_id"
          done

  docker-build-test:
    permissions:
      contents: read
    needs: build-and-test
    runs-on: ubuntu-latest
    if: github.ref != 'refs/heads/main'

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Extract short SHA
        id: vars
        run: echo "SHA_SHORT=$(git rev-parse --short HEAD)" >> $GITHUB_ENV

      - name: Build Docker image
        run: docker build -t spdeepak/aegis:${{ env.SHA_SHORT }} .

  docker:
    permissions:
      contents: read
      packages: write
    needs: build-and-test
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Docker Hub
        run: echo "${{ secrets.DOCKER_PASSWORD }}" | docker login -u "${{ secrets.DOCKER_USERNAME }}" --password-stdin

      - name: Build and push multi-arch Docker image
        run: docker buildx build --platform linux/amd64,linux/arm64 -t spdeepak/aegis:latest --push .

  docker-web-build-test:
    permissions:
      contents: read
    needs: build-and-test
    runs-on: ubuntu-latest
    if: github.ref != 'refs/heads/main'

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Extract short SHA
        id: vars
        run: echo "SHA_SHORT=$(git rev-parse --short HEAD)" >> $GITHUB_ENV

      - name: Build frontend Docker image
        run: docker build -f Dockerfile.web -t spdeepak/aegis-web:${{ env.SHA_SHORT }} .

  docker-web:
    permissions:
      contents: read
      packages: write
    needs: build-and-test
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Docker Hub
        run: echo "${{ secrets.DOCKER_PASSWORD }}" | docker login -u "${{ secrets.DOCKER_USERNAME }}" --password-stdin

      - name: Build and push multi-arch frontend Docker image
        run: docker buildx build -f Dockerfile.web --platform linux/amd64,linux/arm64 -t spdeepak/aegis-web:latest --push .
